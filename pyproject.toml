[tool.ruff]
line-length = 100
target-version = "py313"
exclude = [".venv", "**/migrations/*.py", "docs/**"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "DJ"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["config", "identity", "health", "catalog", "operations", "audit"]
